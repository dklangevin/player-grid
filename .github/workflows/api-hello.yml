name: api-hello
on:
  schedule:
    - cron: '40 * * * *'
# on: [push]
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call teams API route
        run: |
          curl --request GET \
          --url 'https://player-grid.vercel.app/api/cron/teams' \
